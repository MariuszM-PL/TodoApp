<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TodoApp.ViewModels"
             xmlns:models="clr-namespace:TodoApp.Models"
             x:Class="TodoApp.Views.TodoPage"
             x:DataType="vm:TodoViewModel"
             Shell.BackButtonBehavior="{BackButtonBehavior IsVisible=False}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Dodaj" Command="{Binding GoToAddPageCommand}">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="&#xff0b;" Color="{DynamicResource Primary}" Size="24" FontFamily="OpenSansRegular"/>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
        <ToolbarItem Text="Ustawienia" Command="{Binding GoToSettingsCommand}">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="&#x2699;" Color="{DynamicResource Primary}" Size="24" FontFamily="OpenSansRegular"/>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
        <ToolbarItem Text="Wyloguj" Command="{Binding LogoutCommand}">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="&#x23FB;" Color="{DynamicResource Primary}" Size="24" FontFamily="OpenSansRegular"/>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <Grid>
        <Grid RowDefinitions="Auto, *, Auto" Padding="20,10,20,10" RowSpacing="15">

            <VerticalStackLayout Grid.Row="0" Spacing="10">
                <Label Text="Moje Zadania" FontSize="24" FontAttributes="Bold" TextColor="{DynamicResource Primary}" Margin="0,10,0,5"/>

                <SearchBar Placeholder="Szukaj zadania..." Text="{Binding SearchText}" />

                <FlexLayout BindableLayout.ItemsSource="{Binding FilterOptions}" Wrap="Wrap" JustifyContent="Start">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vm:FilterOption">
                            <Border StrokeShape="RoundRectangle 15" Padding="12,6" StrokeThickness="0" Margin="0,0,8,8">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TodoViewModel}}, Path=SelectFilterCommand}" CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>
                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="BackgroundColor" Value="{DynamicResource Primary}" />
                                    </DataTrigger>
                                    <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="False">
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#E0E0E0, Dark=#333333}" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Label Text="{Binding Name}" FontSize="12" FontAttributes="Bold">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="TextColor" Value="White" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </FlexLayout>
            </VerticalStackLayout>

            <CollectionView Grid.Row="1" ItemsSource="{Binding Tasks}" SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="12" />
                </CollectionView.ItemsLayout>
                <CollectionView.EmptyView>
                    <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Opacity="0.5" Spacing="10">
                        <Label Text="&#x1f4ca;" FontSize="40" HorizontalOptions="Center"/>
                        <Label Text="Brak zadań do wyświetlenia" HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:TodoItem">
                        <Frame Margin="2,0" Padding="15" HasShadow="True" CornerRadius="15" 
                               BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}" 
                               BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#2B2B2B}">
                            <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="15">
                                <Border Grid.Column="0" HeightRequest="28" WidthRequest="28" StrokeShape="RoundRectangle 14" StrokeThickness="2" VerticalOptions="Center">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TodoViewModel}}, Path=ToggleDoneCommand}" CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                    <Label Text="✓" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" FontSize="16" IsVisible="{Binding IsDone}"/>
                                    <Border.Triggers>
                                        <DataTrigger TargetType="Border" Binding="{Binding IsDone}" Value="True">
                                            <Setter Property="BackgroundColor" Value="#2ECC71" />
                                            <Setter Property="Stroke" Value="#2ECC71" />
                                        </DataTrigger>
                                        <DataTrigger TargetType="Border" Binding="{Binding IsDone}" Value="False">
                                            <Setter Property="BackgroundColor" Value="Transparent" />
                                            <Setter Property="Stroke" Value="{DynamicResource Primary}" />
                                        </DataTrigger>
                                    </Border.Triggers>
                                </Border>

                                <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                                    <Label Text="{Binding Category}" FontSize="10" TextColor="{Binding CategoryColor}" FontAttributes="Bold"/>

                                    <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding IsDone}" Value="True">
                                                <Setter Property="TextDecorations" Value="Strikethrough" />
                                                <Setter Property="TextColor" Value="Gray" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>

                                    <Label Text="{Binding Description}" FontSize="13" Opacity="0.7">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding IsDone}" Value="True">
                                                <Setter Property="TextDecorations" Value="Strikethrough" />
                                                <Setter Property="TextColor" Value="Gray" />
                                                <Setter Property="Opacity" Value="0.4" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>

                                    <Label Text="{Binding DueDate, StringFormat='{0:dd/MM/yyyy HH:mm}'}" FontSize="11" Opacity="0.6">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding IsDone}" Value="True">
                                                <Setter Property="TextDecorations" Value="Strikethrough" />
                                                <Setter Property="TextColor" Value="Gray" />
                                                <Setter Property="Opacity" Value="0.4" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                </VerticalStackLayout>

                                <HorizontalStackLayout Grid.Column="2" Spacing="2">
                                    <ImageButton Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TodoViewModel}}, Path=TapCommand}" CommandParameter="{Binding .}" HeightRequest="35" WidthRequest="35" BackgroundColor="Transparent">
                                        <ImageButton.Source>
                                            <FontImageSource Glyph="&#x270e;" FontFamily="OpenSansRegular" Color="{DynamicResource Primary}" Size="18"/>
                                        </ImageButton.Source>
                                    </ImageButton>
                                    <ImageButton Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TodoViewModel}}, Path=DeleteTaskCommand}" CommandParameter="{Binding .}" HeightRequest="35" WidthRequest="35" BackgroundColor="Transparent">
                                        <ImageButton.Source>
                                            <FontImageSource Glyph="&#x1f5d1;" FontFamily="OpenSansRegular" Color="Red" Size="18"/>
                                        </ImageButton.Source>
                                    </ImageButton>
                                </HorizontalStackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <HorizontalStackLayout Grid.Row="2" HorizontalOptions="Center" Opacity="0.4" Spacing="8" Margin="0,5">
                <Label Text="TodoApp 2026" FontSize="11" VerticalOptions="Center"/>
                <Image Source="logo.png" HeightRequest="15" WidthRequest="15" Aspect="AspectFit"/>
            </HorizontalStackLayout>
        </Grid>

        <Button Text="+"
                Command="{Binding GoToAddPageCommand}"
                FontSize="28"
                FontAttributes="Bold"
                TextColor="White"
                BackgroundColor="{DynamicResource Primary}"
                HeightRequest="60"
                WidthRequest="60"
                CornerRadius="30"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="0,0,30,30"
                ZIndex="500">
            <Button.Shadow>
                <Shadow Brush="Black" Offset="0,5" Radius="10" Opacity="0.4"/>
            </Button.Shadow>
        </Button>

        <Border x:Name="NotificationToast" VerticalOptions="Start" HorizontalOptions="Center" Margin="20,10" Padding="20,12" TranslationY="-500" BackgroundColor="{DynamicResource Primary}" StrokeShape="RoundRectangle 20" ZIndex="1000" HeightRequest="70" WidthRequest="350">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,10" Radius="20" Opacity="0.3"/>
            </Border.Shadow>
            <Grid ColumnDefinitions="Auto, *" ColumnSpacing="15">
                <Label Text="&#x1f514;" FontSize="24" VerticalOptions="Center" FontFamily="OpenSansRegular"/>
                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                    <Label Text="Przypomnienie!" FontAttributes="Bold" TextColor="White" FontSize="14"/>
                    <Label Text="{Binding CurrentNotificationTask.Title}" TextColor="White" FontSize="12" LineBreakMode="TailTruncation"/>
                </VerticalStackLayout>
            </Grid>
        </Border>
    </Grid>
</ContentPage>